export enum TrafficType {
  train = 1,
  plant = 2,
  bus,
  ship,
}

export enum ActivityType {
  scene,
  traffic,
  shop,
  freeAct,
  specialMeal,
  feeSelf,
  other,
  backTrip,
  trip,
}

export enum PriceType {
  marketPrice,
  distributePrice,
}
